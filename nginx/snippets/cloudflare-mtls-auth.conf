# /etc/nginx/snippets/cloudflare-mtls-auth.conf
# 核心安全策略：强制进行基于证书的双向认证 (mTLS)

# 指定用于验证Cloudflare客户端证书的CA
ssl_client_certificate /etc/nginx/ssl/cloudflare_aop_ca.pem;

# 强制进行客户端证书验证
ssl_verify_client on;

# 验证深度
ssl_verify_depth 1;
